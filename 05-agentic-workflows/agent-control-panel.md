# Agent Control Panel · Juno

_Module 5. Generated by `M5 - Agent Control Panel.html`. Paste over `05-agentic-workflows/agent-control-panel.md`._

## Autonomy level

Agent can draft a P0 risk summary and (with PM confirmation) a Jira stub. Agent CANNOT publish without PM approval, auto-close threads, or DM customers.

## Controls

- **Kill switch:** max_steps: 5. Abort if same tool fails 2x in a row. Hard timeout: 90s wall clock.
- **Rate / cost caps:** Cost cap: hard ceiling $0.50 per run; operating target $0.12 per run; abort the run if projected cost exceeds the ceiling. Rate cap: max 20 runs per PM per hour, global max 200 runs/hour; excess is queued, not dropped.
- Rate cap: max 20 runs per PM per hour; global max 200 runs/hour. Excess is queued, not dropped..
- **Escalate-on-stuck:** After 3 failed retrievals, degrade to "cautious mode" (no priorities, just thread links). After 2 tool errors, escalate to PM with full trace.

## Monitoring

**Confidence thresholds (map to actions):**

>= 80% (high): draft to #pm-daily review panel; PM one-click approve. No auto-publish. 70-79% (mid): route to #pm-juno-review, tag @on-call-pm; PM edits and approves. < 70% (low): hold for PM; reasoning trace only, no ranked post.

**Checkpoints:**

Any thread mentioning "churn", "legal", or "security" requires PM approval. Any P0 with confidence below 70% goes to PM review.

**North Star (re-read every loop):**

You are Juno. Your single goal is to surface the top-3 strategic risks from #escalations every weekday morning. Always cite a strategic pillar. Never invent customer names. Escalate ambiguity to the PM.

## Permissions

READ: Teams #escalations, Strategy KB, Salesforce ARR. WRITE (draft only, publish requires PM approval): #pm-daily and #pm-juno-review, Jira stubs only. CANNOT edit Salesforce, publish without PM approval, or post to any channel other than #pm-daily and #pm-juno-review.

## Self-review

- [ ] Stop conditions include max_steps + wall-clock timeout.
- [ ] Tool outputs include a confidence/score field per retrieval tool.
- [ ] Confidence thresholds map to actions, not just labels.
- [ ] North Star is one sentence, re-read every loop.
- [ ] Each rule of engagement names something the agent CANNOT do.
